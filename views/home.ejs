<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.3/normalize.min.css">
    <link rel="stylesheet" href="styles.css">
    <title>Home Page</title>
</head>
<body>
    <h1>Guesspionage</h1>
    <div class="container">
        <a href="baseQs.ejs">
            <button class="playGame"></button>
        </a>
    </div>
    <h3>To Start The Game Click The Button Below </h3>

    <div class="logout-container">
        <form class="LOut" action="/logout" method="POST"> 
            <button type="submit" value="Logout" class="logout" data-target="/baseQs.ejs"></button>
        </form>
    </div>

    <script>
    $('.logout').on('click', function(event) {
        event.preventDefault(); 
        const url = $(this).data('target');
        location.replace(url);
    });
    </script>